node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Runtime/cache files
onchain-cache.json
automated-analysis.json
notifications-config.json
.zscripts/dev.pid
.zscripts/dev.log
.zscripts/mini-service-*.log
preview-*.png
tool-results/
# OPT-3d: do NOT ignore .github/workflows/ci.yml (was previously excluded,
# preventing CI from being tracked in git). Removed so the CI pipeline ships.
postcss.config.mjs

# FIX-D-11: vendored ClawHub skill catalogs (unrelated to ZAYTRIX app, ~MBs of bloat).
# Files kept on disk for the skill system but untracked from git.
skills/

# SEC-1: user databases are NEVER tracked (db/custom.db contained real
# credentials + PII and was committed once — purged 2026, rotate all secrets).
# FUNC-13: dead artifacts removed from tracking
mini-services/
# runtime artifacts

# SEC-1: user databases are NEVER tracked (db/custom.db pernah berisi
# kredensial nyata — purge 2026, rotasi semua secret).
db/
db/custom.db
.zscripts/
# QA3-F2: smoke test artifacts (screenshots, result JSON, snapshot dumps)
qa-artifacts/
.qa-tmp/
